/* Crie 2 arquivos JavaScript.
  * O primeiro, ira exportar uma função chamada getFlag()
  * que recebera um argumento do tipo String.
  * Essa função devera buscar dentro do array process.argv a flag desejada, que 
  * é a String e retornar o valor da flag
  * 
  * O segundo ira importar a função e passar a flag desejada.
  * 
  * Iremos rodar no terminal o segundo arquivo passando as flags --name e 
  * --greeting para que seja impresso no terminal a saudação e o nome da pessoa.
*/

//node greetings --name "Gabriel Barbosa" --greeting "Tudo bem com você?"